#!/bin/zsh

# The code on Line 1 indicates what type of file this is. It is important to include it in every
# shell script.

# We use a single hashtag to comment.
# To comment out multiple lines, you must append a hashtag on each line.

# The echo command will allow us to output text to the console.
echo Hello World!
